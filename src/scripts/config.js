const config = {
    BASE_URL: 'https://story-api.dicoding.dev/v1',
    MAPTILER_API_KEY: 'YOUR_MAPTILER_API_KEY', // Get this from STUDENT.txt if you use MapTiler
    AUTH_KEY: 'dicoding_story_app_token', // Kunci untuk localStorage
};

export default config;